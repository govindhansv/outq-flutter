import 'package:flutter/material.dart';

class OwnerViewSettingsPage extends StatelessWidget {
  const OwnerViewSettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Owner View Settings"),
      ),
    );
  }
}